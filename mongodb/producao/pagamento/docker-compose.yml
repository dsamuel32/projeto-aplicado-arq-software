version: '2'
services:

  projeto-aplicado-pagamentodb:
    container_name: projeto-aplicado-pagamentodb
    image: projeto-aplicado-pagamentodb
    build: .
    ports:
      - "27017:27017"
#    volumes:
#    - ./config:/config
    network_mode: "projeto-aplicado"