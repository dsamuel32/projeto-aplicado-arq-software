version: '2'
services:

  projeto-aplicado-cadastrodb:
    container_name: projeto-aplicado-cadastrodb
    image: projeto-aplicado-cadastrodb
    build: .
    ports:
      - "27017:27017"
#    volumes:
#    - ./config:/config
    network_mode: "projeto-aplicado"