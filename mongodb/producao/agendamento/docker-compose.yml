version: '2'
services:

  projeto-aplicado-agendamentodb:
    container_name: projeto-aplicado-agendamentodb
    image: projeto-aplicado-agendamentodb
    build: .
    ports:
      - "27017:27017"
#    volumes:
#    - ./config:/config
    network_mode: "projeto-aplicado"